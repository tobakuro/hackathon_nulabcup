# Server
SERVER_PORT=8080

# PostgreSQL
# devbox環境ではUNIXソケット接続を使用
# DB_HOST にソケットディレクトリを指定する（devbox: $PGHOST）
DB_HOST=
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=hackathon
DB_SSLMODE=disable

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0
